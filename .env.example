# MOT Hashcat Playground — Environment Variables
# Copy this file to .env and adjust as needed

# Database URL (SQLite — use a file path)
# On Linux/macOS: file:./db/custom.db (relative) or file:/absolute/path/custom.db
# On Windows: file:./db/custom.db (use forward slashes)
DATABASE_URL=file:./db/custom.db
