# MOT Hashcat Playground — Environment Variables
# Copy this file to .env and adjust as needed

# Database URL (SQLite — use a relative file path)
# The default path works on Linux, macOS, and Windows
# The db/ directory is created automatically by the postinstall script
DATABASE_URL=file:./db/custom.db
